package tnggg;

import org.openqa.selenium.By;

import org.openqa.selenium.WebDriver.Navigation;
import org.openqa.selenium.WebElement;
import org.testng.annotations.Test;


public class cr7 extends baseclasssss{
	@Test
	private void lauch() {
		lauchbrower();
		lauchurl("https://www.facebook.com/");
		loginssssss f=new loginssssss();
		userlogin(f.getUsername(), "kjjghff");
	driver.navigate().refresh();
    userlogin(f.getUsername()," value");
    userlogin(f.getUserpass(), "vicky");
	btn(f.getButton());	
		}
	    }



